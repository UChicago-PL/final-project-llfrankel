COMPLETED

Progress so far has been good. I have:

- Concretized the WEFT IR (descriped in `json-specs.md`).
  - I needed to do this first in order to be able to process the IR, etc

- Defined ADTs for each type of node in my IR (in `IR.hs`).

- Defined Aeson `toJSON` and `fromJSON` for each node type in the IR

- Checked that my `toJSON` and `fromJSON` worked by diffing actual JSON input

- Implemented the basic effect algebra in `Annotate.hs`



NEXT STEPS

My immediate next steps are:

- I suppose I should test edge cases etc for my IR processing

- Processing a program and its metadata to build a dependency graph (`DepGraph.hs`)

- Processing a dependency graph to determine partitions (`Partition.hs`)

- Using partitions to complete annotations of the IR nodes (`Annotate.hs`)


FURTHER OUT

After the above tasks are completed, I plan to

- Implement QuickChecking. We are now talking about this in class (I believe on Wednesday Feb 25), so that will be helpful. And it will illuminate exactly how much work I need to do.

- It also seems to me that I could simplify the IR significantly (reducing the number of nodes). Perhaps I could implement this as a pass over the IR in Haskell to see if it maintains all of the invariants I want. If it does, then I could add it to my actual Swift implementation.
  - It would be cool to use the Haskell implementation to be confident that lower-level changes will not cause issues higher up.
